{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run with xvfb",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args": [],
            "env": {
                "DISPLAY": ":0"
            },
            "preLaunchTask": "Run with xvfb"
        }
    ]
}
